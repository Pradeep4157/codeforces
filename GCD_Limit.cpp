#include <bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:

    there is some array..

    pi is prefix gcd of array...

    f(a) is count of indexes where

    p[i]!=p[i-1].

    when will this happen ?

    obv after every index p[i] is going to dec only...

    the only way when pi == pi-1 when arr[i] == x*p[i-1]

    else p[i] decreses..


    m^n arrays are

    [1,1,1,...(ntimes)]
    [1,1,1,.....2]
    [1,1,1,1..2,2]
    ....
    [m,m,m,m,m,m]





*/
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
    }
    return 0;
}
