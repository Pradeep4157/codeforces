#include <bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:


*/
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int n, m;
    cin >> n >> m;
    vector<vector<char>> adj(n,);
    for (int i = 0; i < m,; i++)
    {   
        int first,second;
        cin>>first>>second;
        
        
    }
    
    return 0;
}
