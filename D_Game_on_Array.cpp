#include<bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:
    
    so here if alice takes any bigger element then bob there is a chance that 
    the number might get reduced and combine with smaller elements 
    to give bob more score same goes for bob 

    so no one is going to take bigger element unless they finish smaller element


    is there any edge case in this :
        1 3 3 
*/
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t; 
    while(t--){

    }
    return 0;
}
