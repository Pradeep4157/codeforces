#include<bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:
    

    obv the edge case is that n == 2 its never possible , but for other n.. 

    so for every element the neighbors are limited.. 

    the range is from i - 4.. i - 2, i + 2.. i + 4 
    
    so the neighbor of the current  element can be only 6 values... 
    
    that is  i - 2, i - 3 , i - 4, i + 2, i + 3, i + 4.. 

    


*/
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t; 
    while(t--){

    }
    return 0;
}
