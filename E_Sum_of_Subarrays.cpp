#include<bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:
    we will search not from l to r because it is 1e5, 
    instead on a map of values

*/
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t; 
    while(t--){
        
    }
    return 0;
}
