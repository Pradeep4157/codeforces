#include<bits/stdc++.h>
#define int long long
using namespace std;
/*
PROBLEM: 

    so the pyramid has center coord..&& H 

    and there is some coord X,Y .. 

    alti of that is max(H-|X-cx|-|Y-cy|,0)

    we need to find cx,cy,h .. 

OBSERVATIONS:
    
    

*/
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t; 
    while(t--){

    }
    return 0;
}
