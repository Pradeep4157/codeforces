#include<bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:
    
    so here for every cell if the cell is black we make n*n black cells else we make n*n first grid that we got 

*/
signed main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t; 
    while(t--){
        int n,k;
        cin>>n>>k;
        
    }
    return 0;
}
