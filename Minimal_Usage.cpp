#include <bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:

    n cakes..

    ith cake has weight i..

    cust wants k cakes with weight == s .. 
    
    for m type cake we are low on ing.. 

    min no of m types that we take.. 

    we init try not to take.. 

    or bs 

    so i will select a req amount of m and then 

    i will reduce that much amount of m from s..

    and then after this on the rem i will see 

    if with k - mid places can i make sum == rem.. 


    how will i say that.. 

    


*/
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
    }
    return 0;
}
