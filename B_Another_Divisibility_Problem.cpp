#include <bits/stdc++.h>
#define int long long
using namespace std;
/*
        OBSERVATIONS:

            IF I ADD XX THEN IT IS DIVISIBLE BY X
*/
signed main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    while (t--)
    {
        int n;
        cin >> n;
        cout << n * 8 << endl;
    }
    return 0;
}
