#include<bits/stdc++.h>
#define ll int64_t
using namespace std;
void recursion(ll n){
    if(n==0) return;
    
}
int main(){
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    ll t;
    cin>>t;
    while(t--){
        ll n;
        cin>>n;
        cout<<recursion(n)<<endl;
    }
}
